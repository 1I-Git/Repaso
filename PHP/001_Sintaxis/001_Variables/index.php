<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Variables</title>
</head>

<body>
    <h1>Variables</h1>
    <?php
    //Variables
    /*
        PHP no es un lenguaje fuertemente tipado.
        El tipo de variable lo asigna el valor.
        Tipos:
        -boolean
        -integer
        -float
        -string
        -array
        -object
        -null (Sin valor)
        
        Ámbito:Una variable declarada en un fichero de PHP está disponible en ese fichero y en los que lo incluyen.
        
        Variables Internas:
        -$GLOBALS Hace referencia a todas las variables disponibles en el ámbito global.
        -$_SERVER Información del entorno del servidor y de ejecución.
        -$_GET Pasar párametros por la URL via método  GET.
        -$_POST Pasar párametros por la URL via método  POST.
        -$_FILES Variables de subida de ficheros HTTP.
        -$_COOKIE Cookies HTTP.
        -$_SESSION Variables de sesión.
        -$_REQUEST Variables HTTP Request. Junta $_GET, $_POST y $_COOKIE.
        -$_ENV  Variables de entorno.
      
      */

    ?>
</body>

</html>